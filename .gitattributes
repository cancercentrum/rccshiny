# Workaround for line ending problems in JSON comparisons in shinytest,
# related to https://github.com/rstudio/shinytest/issues/270
#
# See also
# https://docs.github.com/en/free-pro-team@latest/github/using-git/configuring-git-to-handle-line-endings#global-settings-for-line-endings

# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Denote all files that are truly binary and should not be modified.
# The workround is to treat JSON as a binary.
*.json binary
